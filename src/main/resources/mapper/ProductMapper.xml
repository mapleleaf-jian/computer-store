<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ms.store.mapper.ProductMapper">
    <resultMap id="ProductEntityMap" type="ms.store.entity.Product">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="itemType" column="item_type"/>
        <result property="title" column="title"/>
        <result property="price" column="price"/>
        <result property="sellPoint" column="sell_point"/>
        <result property="num" column="num"/>
        <result property="image" column="image"/>
        <result property="status" column="status"/>
        <result property="priority" column="priority"/>
        <result property="createdUser" column="created_user"/>
        <result property="createdTime" column="created_time"/>
        <result property="modifiedUser" column="modified_user"/>
        <result property="modifiedTime" column="modified_time"/>
    </resultMap>

    <select id="findHotList" resultMap="ProductEntityMap">
        select * from t_product order by priority desc
    </select>

    <select id="findLatestList" resultMap="ProductEntityMap">
        select * from t_product order by created_time desc
    </select>

    <select id="findProductById" resultMap="ProductEntityMap">
        select * from t_product where id=#{id}
    </select>

    <update id="updateNumByPid">
        update t_product
        set num=#{num}, modified_user=#{modifiedUser}, modified_time=#{modifiedTime}
        where id=#{id}
    </update>
</mapper>